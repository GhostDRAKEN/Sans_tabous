import 'package:flutter/material.dart';
class AnatomieSexuelle extends StatelessWidget{
  const  AnatomieSexuelle({super.key});
  @override
  Widget build(BuildContext context) {
  return Scaffold(
    appBar: AppBar(
      title: Text('Anatomie Sexuelle'),
    ),
    body: Center(
        child: Text('Contenu '),
  ),
  );
  }

}