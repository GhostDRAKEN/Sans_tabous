import'package:flutter/material.dart';
class ContraceptionPage extends StatelessWidget {
  const ContraceptionPage ({super.key});
  @override
 Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text('Contracption'),
      ),
      body: Center(
        child: Text('Contenu'),
      ),
    );
  }
}