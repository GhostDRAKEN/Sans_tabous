import 'package:flutter/material.dart';
class RespectPage extends StatelessWidget {
  const RespectPage({super.key});
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text('Respect des choix'),
      ),
      body: Center(
        child: Text('Contenu '),
      ),
    );
  }
}